namespace SharpNeedle.Framework.HedgehogEngine.Mirage.ShaderData;

public enum ShaderByteCodePlatform : byte
{
    None = 0,
    Windows = (byte)'w',
    Xbox = (byte)'x',
    Playstation = (byte)'p',
}
